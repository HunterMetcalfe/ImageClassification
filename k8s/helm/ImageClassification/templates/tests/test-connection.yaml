apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "ImageClassification.fullname" . }}-test-connection"
  labels:
    {{- include "ImageClassification.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "ImageClassification.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never

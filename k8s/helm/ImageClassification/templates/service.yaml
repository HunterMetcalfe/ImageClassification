apiVersion: v1
kind: Service
metadata:
  name: {{ include "ImageClassification.fullname" . }}
  labels:
    {{- include "ImageClassification.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ImageClassification.selectorLabels" . | nindent 4 }}
